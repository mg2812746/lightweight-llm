# main.py

import config.config
import embedding.main
import tokenize.main
import transformers.main

# Access configuration variables and hyperparameters
print(config.INPUT_VOCAB_SIZE)
print(config.NUM_LAYERS)
